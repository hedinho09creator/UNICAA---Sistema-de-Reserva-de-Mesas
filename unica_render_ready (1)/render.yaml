services:
  - type: web
    name: unica-web
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: RENDER
        value: "true"
      - key: SECRET_KEY
        generateValue: true
